"use client";
import { useEffect, useState } from "react";
import { getPortfolioSummary } from "@/lib/api";
export default function PortfolioPage() { const [summary, setSummary] = useState<any>(null); useEffect(() => { getPortfolioSummary().then(setSummary); }, []); return (<div className="space-y-6"><div className="card"><h3 className="text-lg font-semibold">Portfolio Overview</h3><p className="mt-2 subtle">Detailed holdings table comes next. This v1 skeleton exposes the summary data first.</p></div><div className="grid gap-4 md:grid-cols-3"><div className="card"><p className="subtle">Portfolio Value</p><div className="metric mt-2">₹{summary?.total_portfolio_value ?? 0}</div></div><div className="card"><p className="subtle">Total P&L</p><div className="metric mt-2">₹{summary?.total_pnl ?? 0}</div></div><div className="card"><p className="subtle">Open Positions</p><div className="metric mt-2">{summary?.open_positions ?? 0}</div></div></div></div>); }
